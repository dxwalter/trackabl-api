import { BadRequestException, UnauthorizedException } from "@nestjs/common"; /**
 * status messages
 * ---------------------
 * Define all your possible custom status messages here.
 */

export const EntrypointStatusMessages = {
  create: {
    success: "Entrypoint data created",
  },
};
